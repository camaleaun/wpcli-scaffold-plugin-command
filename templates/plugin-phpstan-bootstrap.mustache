<?php
/**
 * PHPStan bootstrap — defines constants required for static analysis.
 */

require_once __DIR__ . '/vendor/symfony/polyfill-php80/bootstrap.php';

define( 'ABSPATH', '/tmp/wordpress/' );
define( 'WPINC', 'wp-includes' );
define( '{{plugin_const_prefix}}_VERSION', '0.1.0' );
define( '{{plugin_const_prefix}}_PATH', __DIR__ . '/' );
define( '{{plugin_const_prefix}}_URL', 'https://example.com/wp-content/plugins/{{plugin_slug}}/' );

if ( ! function_exists( 'selfd' ) ) {
	function selfd( string $file ): void {} // phpcs:ignore
}
